'use client';

// Collection action bar — currently empty (visibility is in badge row, share removed).
// Dealer-specific actions (move to sales, etc.) will be added here.
export function CollectionActionBar() {
  return null;
}
